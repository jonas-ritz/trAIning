# Content
This directory holds tools to analyze fit files and the content that is inside.

It is distinguished between strength anyalyis (fit files that hold a gym workout) and cardio files (fit files that hold a cardio session like biking, running or swimming).